{
  "name": "ringweave",
  "version": "0.1.0",
  "description": "Ring-greedy generation of near-regular graphs with minimal average shortest path length, with hard/soft constraints. Powers the BuddyGraph app. Zero dependencies.",
  "type": "module",
  "author": "Patrick Sharp (github: sharpTrick)",
  "license": "MIT",
  "main": "dist/core/index.js",
  "types": "dist/core/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
